#include <iostream>
using namespace std;
int main()
{
   double inc;
   inc = 40;
   if (inc == 50)
      cout << " very good";
   else if (inc == 40)
      cout << "good";
   else if (inc == 30)
      cout << "satisfactory";
   else if (inc == 20)
      cout << "go ahead";
   return 0;
}